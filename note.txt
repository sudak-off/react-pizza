

npm add node-sass
npm add classnames

componentDidMount()
componentDidUpdate()

props.children


class Button extends React.Component {


    render() {
        return (<button className={classNames(
            'button',
            {
                "botton--outline": this.props.outline,
            },
            {
                "botton--filled": this.props.children,
            },
        )} >{this.props.children}</button>
            // <button>{this.props.text}</button>
        );
    }

}